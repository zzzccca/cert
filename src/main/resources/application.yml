spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/certificate?characterEncoding=utf-8&useSSL=false
    username: root
    password: wuliancan
server:
  port: 8090